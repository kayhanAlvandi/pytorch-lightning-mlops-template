Add GHCR image pipeline, registry-backed compose, and MLOps phase docs
 
- Reusable build-push-image composite action: GHCR login via token input,
  buildx, docker/metadata-action tags (latest/sha/semver/extra_tag), GHA cache
- Build-push jobs in ci_serving/ci_monitoring (gated on tests, push to main
  only) and ci_training (workflow_dispatch, requires run_tests=all, manual
  image_tag input); standalone cd_mlflow workflow for the mlflow image
- Production compose files pull GHCR images (api, monitoring, mlflow,
  compute_references); train, build_dataset, and serve-dev stay local builds
- Bake training code into train image; split build_dataset into a lightweight
  standalone image/compose/env without CUDA deps
- Makefile: NAME/MANIFEST/ARGS params instead of hardcoded values
- Add docker/IMAGES.md and phase planning docs (kubernetes, airflow/terraform)