# Docker Compose variable substitution (auto-loaded by docker compose)
# Copy this file to .env and set values for your local setup

# Drive mount path (host path to O: drive)
O_DRIVE_PATH=/mnt/O

# Path to the shared 'tools' library on host
TOOLS_LIB_PATH=/path/to/your/tools/library
