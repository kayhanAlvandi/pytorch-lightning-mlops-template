# Monitoring jobs (drift report, quality report, label backfill, benchmark
# registration). No torch/timm/mlflow -- run_id is read off the running API's
# /model endpoint via stdlib urllib, and compute_reference/compute_benchmark
# (real inference) reuse the api image separately.
#
# Evidently is pinned to the last release of the classic
# evidently.report.Report / metric_preset / as_dict() API (0.7.x is a rewrite
# with a different, incompatible API that run_drift_report.py does not use).
evidently==0.6.7
pandas>=2.0.0
psycopg[binary,pool]>=3.2.10
pydantic-settings>=2.0.0
# Label backfill + benchmark registration (tools.loading.getCategories runtime deps)
pymongo>=4.6
joblib>=1.3
# Benchmark registration (register_benchmark.py reads image shapes, no inference)
pillow>=10.0.0
pillow-jxl-plugin>=0.10.3
