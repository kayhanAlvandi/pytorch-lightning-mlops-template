_target_: torch.optim.SGD
learning_rate: 0.001
weight_decay: 0.0001
momentum: 0.9
nesterov: true

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  mode: min
  factor: 0.5
  patience: 5
