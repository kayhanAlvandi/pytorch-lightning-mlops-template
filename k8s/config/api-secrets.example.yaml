# Template -- copy to api-secrets.yaml and fill in real values.
# api-secrets.yaml is gitignored (contains DB credentials); this file is not.
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: api
type: Opaque
stringData:
  API_DB_URI: "postgresql://<user>:<password>@postgres.api.svc.cluster.local:5432/prediction"
